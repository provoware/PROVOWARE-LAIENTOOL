# EV-20260921-002 – B01-A Capability-/Preflight-Vertrag

- **Datum:** 2026-09-21
- **Status:** PASS für Code-/CI-Vertrag; physische Zweitgeräte-Prüfung OPEN
- **Commit/Fingerprint:** wird an den Merge-Commit des PR gebunden
- **Bezug:** REQ-001, REQ-003, REQ-004; AQ-003, AQ-010, AQ-013, AQ-020
- **Umgebung:** GitHub Actions Ubuntu 24.04 + Standard-Python

## Erwartung

Der Starter erkennt die notwendigen lokalen Fähigkeiten selbst, entscheidet deterministisch über den sichersten verfügbaren Startmodus und verändert das System nicht.

## Automatisierte Prüfung

```bash
PYTHONPATH=src python3 -m unittest discover -s tests -v
python3 start.py
python3 start.py --json
```

## Belegte Fälle

- os-release-Parsing;
- XDG-Downloads mit Leerzeichen;
- Fallback `~/Downloads`;
- Runtime-Klassifizierung;
- Capability-Erfassung;
- GUI-ready bei vollständigen lokalen Voraussetzungen;
- CLI-Fallback bei fehlendem PySide6;
- blocked-readonly bei fehlender Kernvoraussetzung;
- bundled Runtime erhält Portabilitätspriorität;
- fremde Distribution bleibt OPEN.

## Sicherheitsbefund

Keine neue Runtime-Dependency, kein Netzwerk, keine Installation, keine Rechteausweitung und keine schreibende Nutzdatenoperation.

## Offene Evidence

Die reale zweite Zielmaschine muss weiterhin physisch geprüft werden. Dafür ist nun ein standardisierter read-only Evidence-Runner vorhanden:

```bash
python3 scripts/second_device_evidence.py --json
```

Der Runner erzeugt keinen automatischen PASS für die zweite Maschine; er bereitet nur den reproduzierbaren, redigierten Nachweis vor. Der physische Zweitgeräte-Status bleibt bis zur realen Ausführung `OPEN`.
