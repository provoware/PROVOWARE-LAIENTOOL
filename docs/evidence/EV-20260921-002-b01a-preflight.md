# EV-20260921-002 – B01-A read-only Preflight

- **Datum:** 2026-09-21
- **Status:** PASS für Code-/CI-Vertrag; physische Zweitgeräte-Prüfung OPEN
- **Commit/Fingerprint:** wird an den Merge-Commit des PR gebunden
- **Bezug:** REQ-001, REQ-003, REQ-004; AQ-010, AQ-013, AQ-020
- **Umgebung:** GitHub Actions Ubuntu 24.04 + Standard-Python

## Erwartung

Ein minimaler Preflight soll lokale Systemdaten lesend erfassen und fehlende/abweichende Voraussetzungen nur melden.

## Prüfung

```bash
PYTHONPATH=src python3 -m unittest discover -s tests -v
python3 start.py
python3 start.py --json
```

## Abnahmekriterien

- keine Runtime-Dependency außerhalb Python-Standardbibliothek;
- kein Netzwerk;
- keine Installation;
- keine Rechteausweitung;
- unbekannte Plattform bleibt `OPEN`;
- Ubuntu/Kubuntu 24.04 wird als initiale Familie erkannt;
- Parser und Evaluierung besitzen Unit-Tests.

## Ergebnis

Die automatisierten Tests belegen den Codevertrag. Die reale zweite Zielmaschine ist noch nicht durch Repository-Evidence belegt und bleibt deshalb ausdrücklich `OPEN`.
